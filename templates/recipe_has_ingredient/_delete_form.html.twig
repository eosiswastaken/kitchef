<form method="post" action="{{ path('app_recipe_has_ingredient_delete', {'id': recipe_has_ingredient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recipe_has_ingredient.id) }}">
    <button class="btn">Delete</button>
</form>

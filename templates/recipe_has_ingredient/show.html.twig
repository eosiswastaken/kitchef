{% extends 'base.html.twig' %}

{% block title %}RecipeHasIngredient{% endblock %}

{% block body %}
    <h1>RecipeHasIngredient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recipe_has_ingredient.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ recipe_has_ingredient.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recipe_has_ingredient_index') }}">back to list</a>

    <a href="{{ path('app_recipe_has_ingredient_edit', {'id': recipe_has_ingredient.id}) }}">edit</a>

    {{ include('recipe_has_ingredient/_delete_form.html.twig') }}
{% endblock %}
